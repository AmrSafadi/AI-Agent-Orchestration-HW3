{
  "version": "1.00",
  "max_total_usd": 2.0,
  "max_total_tokens": 100000,
  "warn_at_percent": 80,
  "notes": "Caps for opt-in real CrewAI runs. orchestration.accounting.budget_alerts enforces the warn/over-budget thresholds and SDK.estimate_cost forecasts cost from config/models.json pricing on the dry-run path."
}
